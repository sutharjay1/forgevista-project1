import React from 'react';

const EventCard = ({
	eventTitle,
	eventDescription,
	eventImg,
	eventDate,
	eventTags,
	eventLocation,
	eventLink,
	eventTime,
	eventPrice,
	eventOrganizer,
	eventPurchaseLink,
}) => {
	return <div></div>;
};

export default EventCard;
